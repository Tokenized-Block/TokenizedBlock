[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

# UNIT TESTS ONLY — NO broadcast / NO mainnet deploy this tip.
# Align decide() with brain-tasks.js decideOffre (THRESHOLD=5, toy weights).
# FEE_WALLET 0x37eb9b7ce0b51fe12fbf092026e001918128580a — do not change.
#
# Run from contracts/:
#   forge build
#   forge test -vv
# Fork / fixtures (O4+) — eth_call only, never --broadcast:
#   forge test --match-contract OfferCircuitFork --fork-url https://mainnet.base.org -vv
