[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

# UNIT TESTS ONLY — NO broadcast / NO mainnet deploy this tip.
# Align decide() with brain-tasks.js decideOffre (THRESHOLD=5, toy weights).
# FEE_WALLET 0xa6cF99D35949c6cB911adB910078F4Ca46F0f5d4 (tip 2220 smart wallet). Live CreateRouter/TbFeeHook still 0x37eb immutable.
#
# Run from contracts/:
#   forge build
#   forge test -vv
# Fork / fixtures (O4+) — eth_call only, never --broadcast:
#   forge test --match-contract OfferCircuitFork --fork-url https://mainnet.base.org -vv
