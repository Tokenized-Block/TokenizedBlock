# HANDOFF — session TBLOCK du 2026-09-25

Reprise dans un terminal Claude neuf. Lis ce fichier en entier **avant** de toucher au code.

---

## 0. L'état, en cinq lignes

| | |
|---|---|
| dépôt | `D:\Users\VolKov\veilleIA\tblock-app` · branche **`feat/auto-salt-create`** (⚠️ pas `main`) |
| arbre | propre, `HEAD == origin/feat/auto-salt-create` |
| tests | **118 fichiers, 118 verts** |
| build servi | `20260925-actions-payables`, vérifié en production |
| session | **19 commits**, tous déployés et vérifiés un par un |

Déploiement : `node vendor-xmtp.mjs` → `railway up --no-gitignore --detach --service tokenized-block`
→ `node mesure-build-servi.mjs https://tokenizedblock.space <empreinte>`.
Le binaire réel : `C:\Users\VolKov\AppData\Roaming\npm\node_modules\@railway\cli\bin\railway.exe`.
⛔ **Toute redéploiement porte une empreinte NEUVE** — sinon la sonde confirme l'ancien build avec
enthousiasme (ça s'est produit, sur un build cassé).

---

## 1. Les règles dures — non négociables

- ⛔ Jamais signer / envoyer / swapper / transférer / créer un token on-chain. Contrainte du
  harnais, non levable. **Ce qui est prouvé s'arrête au plan.**
- ⛔ Jamais de clé privée / CDP / Stripe, `.env` compris. Jamais imprimer d'adresse personnelle.
- ⛔ **Jamais compléter une adresse ou un hash de mémoire.** Quatre caractères de fin ne sont pas
  uniques : un block finissant en `…0601` s'est révélé être CASHBOOK, pas celui de la capture.
- ⛔ Commits par `git commit -F <fichier>`, pathspec explicite, jamais `git add -A`.
  **Erreur commise aujourd'hui** : un `-m` avec des accents graves a fait exécuter des morceaux du
  message par le shell. Corrigé par `--amend -F`.
- ⛔ Édition par Write/Edit, **jamais par chaîne shell** (une passe PowerShell a déjà corrompu
  l'encodage en production).
- ⛔ `git pull --ff-only` avant tout push.
- ⛔ Ne pas trancher seul une sémantique produit. Dire, puis attendre.
- ⛔ Français dans les commentaires, **anglais dans tout texte affiché**.

## 2. La discipline qui a produit tous les résultats du jour

1. **Mesurer avant de croire.** Aucune conclusion ne dépasse sa mesure.
2. **Rouge prouvé par mutation.** Une garde qui n'a jamais rougi ne garde rien. 12 mutations
   vérifiées aujourd'hui.
3. **Écrire dans chaque instrument la borne de ce qu'il peut prouver** (`⚠️ NE PROUVE PAS…`).
4. **Se corriger à voix haute.** Cinq rétractations aujourd'hui, listées §5.
5. **Distinguer « lecture ratée » de « rien à lire ».** Le motif n°1 du dépôt — trouvé **quatre
   fois** aujourd'hui, à quatre endroits différents.
6. **La copie qu'on ne relit pas reste fausse** (`single-and-batch-twins-diverge`) : trouvé
   trois fois.

---

## 3. Les prises du jour, par ordre de gravité

### ⛔⛔⛔ Un nom de paramètre tuait la mise en vie
`signerEtapeLancement(etape)` masquait la fonction `etape()` du compteur. `etape('vie_echec')`
levait `TypeError`, donc : aucun message, `vieAutoArreter()` jamais atteint, aucun compteur.
L'appel est en `void …` → promesse non gérée, **silencieuse**. Quelqu'un qui refusait une signature
restait devant « Step 2 — … Your wallet opens now. » **pour toujours**.
Et ça explique un trou dans les chiffres : `vie_ko_etape1` et `vie_ko_etape4` existaient, **jamais
`vie_ko_etape2`** — alors que l'étape 2 porte 3 des 5 signatures. Ce n'était pas « personne
n'échoue là », c'était « là, ça ne peut pas se compter ».
⚠️ **Les 4 blocks créés-jamais-vivants ne peuvent PAS être attribués sur les compteurs actuels.**

### ⛔⛔ Le Bridge annonçait 0,01 % et prélevait 0,5 % — facteur 50
18 littéraux du tarif dans `app.html` + `bridge.js`, pendant que `BRIDGE_FEE_BPS = 50n`.
**Trois assertions vertes le verrouillaient**, dont deux qui passaient encore après correction —
satisfaites par mes propres commentaires citant le défaut retiré.
Tout passe désormais par `BRIDGE_FEE_LABEL`, peint dans des emplacements `data-frais-bridge` qui
valent **« — »** au repos : un prix manquant est honnête, un prix faux ne l'est pas.
Et le bouton « Confirm Bridge fee » **vendait les blocks** → renommé.

### ⛔⛔ Une exception tuait la carte pour toujours
`anim = requestAnimationFrame(pas)` était la **dernière** instruction : si `image()` jetait, la
boucle ne se replanifiait plus, et `anim` restant truthy, les trois relances étaient des no-op.
Tous les cubes figés à leur dernière position — ceux près du bord, collés au bord.
Le jumeau dans `soleilsSurLaMap` était **déjà** sous try/catch.

### ⛔⛔ Le chemin qui rapporte refusait en silence
Balayage des 317 fonctions : `preparerEchange` premier du classement, **10 sorties muettes**, toutes
avant `achat_prepare`. Dont **les deux gardes du 0,5 %** — un échange perdu parce que nos frais
manquaient était indistinguable de « personne n'a essayé ».
→ 11 compteurs `echange_refus_*`, groupés par réparation.

### ⛔⛔ 31 clics sur Create sans aucune trace
`create_clic` 45 · `cree` 6 · `cree_echec` 8. **J'ai annoncé 3 sorties muettes ; la sonde en a
trouvé 16.** → 8 compteurs `cree_refus_*`.

### ⛔⛔ Les actions tokenisées : audit complet
**Ce qui marche** (mesuré on-chain, bloc 51 785 860) : 13/13 adresses concordent, **13/13 à 8
décimales**, hook V8 les admet, écart 10^10 correctement géré (237 190 ticks mesurés).
**Ce qui était cassé, maintenant réparé** :
- Buy/Sell refusait tout marché non-USDC → ouvert **là où un marché est mesuré** (8 des 13 ont un
  prix lisible). ⛔ Pas en aveugle : a6cf a déjà été payé en jetons invendables (7 détentions, 0
  avec un marché, part réelle 0 $). **Deux verrous indépendants** — forcer le premier fait tomber
  le second, prouvé par mutation.
- Le frais était imprimé « en ETH à 18 décimales » alors qu'il est libellé dans la devise du
  marché : **facteur 10^10 et faux nom**, sorti avec la porte.
- **Le préflight exigeait un seed ETH inexistant** → refusait des comptes solvables. Un seul `<=`
  séparait « payable » de « refusé ». `null` = pas su lire ; `0n` = il n'y en a pas.
- Le champ « ETH seed » restait visible **et rempli** sur une paire action.
  `estNaissanceInstantaneeUi()` existait pour ça et n'était appelée nulle part.
- La paire gravée était **perdue à la reprise** : un block créé en INTCc rouvrait un marché ETH, en
  contradiction définitive avec son `contractURI`. Une pool ne se ré-initialise pas.

### ⛔ Le cerveau, deux défauts
- Ta capture (6 ticks, 0 spike, carré noir) était **honnête** : sans marché ni nourriture, le
  premier neurone tire entre le 7ᵉ et le 10ᵉ battement. Le défaut était le **silence** — « en
  charge », « mort » et « canvas cassé » donnaient le même écran.
- **Le marché lu hors carte était jeté** : 385 blocks au sélecteur, 172 en ⏳ dont 162 sur aucune
  carte. Et `brainNourriture` existait (ajouté le matin) **sans `brainMarche`** — mon propre
  correctif à moitié livré, le même jour.

### ⛔ Autres
- `holdersCache` était en mémoire pure → rejeu depuis la naissance à **chaque** redéploiement.
  Persisté sur `/data`, seules les passes **complètes**, écriture atomique, borné à 4 Mo,
  **échoue ouvert** (un incident passé du dépôt est un volume plein qui a corrompu une base).
- L'écran **accusait** un détenteur du rôle d'émission sans l'avoir lu (seul `supply` vs `cap` est
  mesuré ; un B20 sans admin n'a aucun rôle).
- Les blocks vivants ne quittent plus la carte (`futVivant`) : `NON_TROUVEE` **n'est pas fiable**,
  le fichier contient déjà un correctif qui le promeut en `LUE`.
- Rôles 7 → 9 (Hunter, Climber), **ajoutés en fin de liste** : `METIERS_DERIVES` prend les cinq
  premiers et le tirage fait `% length` — élargir réattribuerait le métier de tous les blocks.
- `stades.js` ne comptait que l'ETH (24 blocs sur 96 lisibles hors ETH, dont un à 155 446 $).
- `vieEnUsd` était la **troisième** copie du même calcul, et elle gouverne la taille des tuiles.
- « our reader did not come back » pour 162 blocks jamais lus → nouveau groupe `PAS_REGARDE`.

---

## 4. Les chiffres mesurés — à ne pas réinventer

**Entonnoir** (persistant depuis 2026-09-19, `/api/entonnoir`) :
`visite 303 · create_clic 45 · cree 6 · cree_echec 8 · vivant 2 · achat 2 · lien_recu 19`.
Trafic : 98 → 70 → 30 → 4 (09-22 au 09-25).

**Paliers** (`pointsdevie.js`, en **dollars** de capitalisation) :
`Seed 0 · Sprout 1k · Branch 10k · Trunk 100k · CANOPÉE 1 M · Forest 100M · Monument 1Md`.
Mesure `/api/trending` (187 lignes, non tronquée) : **Sprout 177 · Branch 9 · Trunk 1 · Canopée 0**.
Plus gros marché : **154 694 $** · médiane 4 185 $. La canopée est à **6,5×** du meilleur block.
⛔ Ce n'est **pas** un bug : rien dans le code ne la bloque.

**Actions Coinbase** : 13 au registre, **8 avec un prix lisible** (AAPLc, AMZNc, GOOGLc, METAc,
MSFTc, NVDAc, SPCXc, TSLAc) · **5 sans** (COINc, CRCLc, INTCc, MSTRc, SNDKc).

**Rail fiat** : VIVANT (clé CDP posée le 09-24, jetons Coinbase frais vérifiés 5×).
⚠️ **0 $ encaissé** — le parcours d'achat n'est pas exercé.

---

## 5. Mes erreurs du jour — retractées, à ne pas rejouer

1. **`moteur3d.retirer` n'existait pas.** Non exporté, appelé derrière un `typeof` → branche morte.
   Mon test était vert parce qu'il cherchait la **chaîne** dans la source.
2. **Le fondu ne fondait pas.** `map3d.js` écrit `style.opacity` en ligne, ça bat la classe.
3. **J'ai annoncé 3 sorties muettes dans Create ; il y en avait 16.**
4. **190 cubes « figés »** — artefact : le panneau navigateur était replié, `innerWidth = 0`,
   rAF suspendu. ⛔ **Toute mesure de géométrie est nulle quand le panneau est masqué.**
5. **Un test de persistance qui ne prouvait rien** : il cherchait un marqueur que le test imprimait
   lui-même. Refait pour démarrer le serveur et interroger la route.

---

## 6. Ce qui reste ouvert — par valeur décroissante

1. ⛔ **Les 5 actions sans prix n'ont aucun plancher anti-brade**, et la case de confirmation dit
   « Below 1 ETH » même quand la valeur est en INTCc. `app.html` ~9009 et ~2288.
2. ⛔ **`goLaunch` démarre DEUX `calculerLancement()` concurrents** : `lancementLien` est posé avant
   `ouvrirProfil`, et la branche deep-link en relance un. L'un meurt sur `lancementEnCours`. Double
   la charge RPC sur un nœud qui étrangle déjà.
3. ⛔ **`habitants.splice(habitants.indexOf(h), 1)` à ~4017** : si `indexOf` rendait `-1`,
   `splice(-1,1)` retire **le dernier habitant**, en silence, sans toucher son DOM → orphelin.
   Non atteignable aujourd'hui (boucle synchrone), armé au premier `await` ajouté.
4. ⛔ **`mesure-sorties-mise-en-vie.mjs` classe la ligne `vie_echec` comme instrumentée** alors
   qu'elle était inatteignable. La sonde verdissait sur un trou.
5. ⛔ **`test-paires.mjs` est annoncé dans `paires.js` et n'existe pas.** Comparaison faite à la
   main (13/13) : la garde manque, pas l'exactitude.
6. ⛔ `'0' * 128` dans `lancer-pool.js:245` — vaut le **nombre** 0, marche par accident.
7. ⛔ `serveur-web.js:269` annonce `fenetres * 2000` blocs balayés ; la boucle avance de **999**.
8. ⛔ `#stades` dit « Open a profile or Retry » et **ne contient aucun bouton**.
9. ⚠️ `#cCreer` est à **2 612 px** au format 375 px. La barre du haut a été rendue statique le 09-23
   sur capture de Phil (elle recouvrait Name/Symbol). Un bandeau d'action fin se justifierait —
   **décision produit**.
10. ⚠️ **La persistance des holders n'a pas encore produit sa première passe complète en prod** :
    CASHBOOK a 4 185 transferts et les redéploiements arrivent plus vite que le rejeu. Prouvée
    localement par mutation, **pas observée en ligne**. Laisser le serveur tranquille pour voir.
11. ⚠️ Le trade sur paire action n'est pas exercé sur la chaîne (harnais). Le plan est prouvé.
12. ⚠️ `utiliseCreateRouter()` rend `true` inconditionnellement sur 8453 → `creerEtVivreUneSignature`
    (~150 lignes) **ne tourne jamais**. Si c'est voulu, l'étiqueter code mort.

---

## 7. Pièges du dépôt — lire avant de coder

- **Une fenêtre de lecture en nombre de caractères pourrit.** Deux tests sont tombés aujourd'hui
  pour ça (`slice(d, d + 4500)`, « fenêtre < 600 caractères »). **Borner par équilibrage
  d'accolades**, jamais par un nombre.
- **Une garde peut être satisfaite par le commentaire qui documente sa propre violation.** Arrivé
  deux fois aujourd'hui, dans un fichier qui décrit ce piège vingt lignes plus bas.
- **Un test peut verrouiller un défaut** en épinglant une valeur d'un jour (`0.01%`, la liste des
  puces, « Confirm Bridge fee »). Épingler la **structure**, pas la mesure.
- **Le panneau navigateur replié → `innerWidth = 0` et rAF suspendu.** Utiliser `read_page` /
  `javascript_tool` sur du texte, jamais de géométrie.
- `publicnode` plafonne à **9 adresses** par `eth_getLogs` ; `/api/cle/` répond « over rate limit »
  très vite. Un refus de nœud **n'est pas un verdict** — un agent a produit 12 fausses divergences
  avant de ralentir.
- Les jetons B20 ont **1 octet de code (`0xef`)** : les lectures ne marchent que sur un nœud Base.

---

## 8. Où regarder d'abord au réveil

```bash
cd /d/Users/VolKov/veilleIA/tblock-app
for f in test-*.mjs; do node "$f" >/dev/null 2>&1 || echo "ROUGE: $f"; done
curl -s https://tokenizedblock.space/api/entonnoir | head -c 1200
curl -s https://tokenizedblock.space/api/holders/0xb20000000000000000000016d09cd53724fc0601
```

Les compteurs à lire en premier, tous posés aujourd'hui et **vides par construction avant ce
matin** : `cree_refus_*` (8), `echange_refus_*` (11), `achat_clic`, `vie_ko_etape2`.
⛔ Les `+1` du 2026-09-25 sur `cree_refus_encours`, `echange_refus_wallet` et `achat_clic` sont
**mes sondes**, pas des visiteurs.
